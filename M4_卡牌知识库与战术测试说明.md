# M4 卡牌知识库与战术测试说明

更新时间：2026-07-06

## 1. 已完成内容

M4 已按 `执行文档.md` 完成：

- `src/cards/card_db.py`：聚合官方 EN/JP CSV，并合并官方引擎 `all_card_data/all_attack` 信息；
- `src/cards/tags.py`：自动效果标签 + manual overrides；
- `data/cards.json`：可供策略/搜索/模型使用的结构化 CardDB；
- `data/card_tags.json`：每张卡的效果标签；
- `data/manual_overrides.json`：主卡组关键卡人工修正；
- `tests/test_card_db.py`：CardDB 与关键标签验收；
- `tests/test_tactics.py`：核心战术 observation 回归测试；
- `jobs/m4_carddb_tests.slurm`：服务器 CPU 测试模板。

## 2. CardDB

来源：

```text
pokemon-tcg-ai-battle/EN_Card_Data.csv
pokemon-tcg-ai-battle/JP_Card_Data.csv
pokemon-tcg-ai-battle/Card_ID List_EN.pdf
pokemon-tcg-ai-battle/Card_ID List_JP.pdf
submission/cg/api.py::all_card_data/all_attack
```

当前统计：

```text
EN CSV rows: 2022
JP CSV rows: 2022
unique Card ID: 1267
engine card count: 1267
engine attack count: 1556
```

说明：CSV 是一张卡多行的攻击/招式表，CardDB 以 `Card ID` 为主键聚合。

生成命令：

```bash
python src/cards/card_db.py --out data/cards.json
```

## 3. Tags

标签包括：

```text
draw, search_pokemon, search_energy, attach_energy, switch, gust,
discard, damage_boost, heal, stadium_control, ace_spec, deck_thinning,
recursion, prize_acceleration, risk_deckout
```

主卡组关键标签已人工校正：

| Card ID | Card | Key tags |
|---:|---|---|
| 3 | Basic Water Energy | basic_energy, water_energy |
| 722 | Snover | basic_pokemon, setup_piece |
| 723 | Mega Abomasnow ex | main_attacker, mega_ex, risk_deckout |
| 1145 | Mega Signal | search_pokemon, deck_thinning |
| 1158 | Maximum Belt | ace_spec, damage_boost, tool |
| 1205 | Cyrano | supporter, search_pokemon |
| 1227 | Lillie's Determination | supporter, draw, hand_refresh |
| 1235 | Waitress | supporter, search_energy, attach_energy |

生成命令：

```bash
python src/cards/tags.py --cards data/cards.json --overrides data/manual_overrides.json --out data/card_tags.json
```

## 4. 战术测试

`tests/test_tactics.py` 当前覆盖：

- 开局 Active 优先 Snover；
- 能进化主攻时优先进化 Mega Abomasnow ex；
- 水能优先贴给可成型主攻；
- 牌库很薄时避免 Hammer-lanche 的弃顶 deck-out 风险；
- 检索时已有 Snover 则优先找 Mega Abomasnow ex。

这次 M4 修正了一个旧问题：Hammer-lanche 的 deck-out 风险惩罚原先方向写反，现在由战术测试锁住。

## 5. 验收命令

```bash
python src/cards/card_db.py --out data/cards.json
python src/cards/tags.py --cards data/cards.json --overrides data/manual_overrides.json --out data/card_tags.json
python tests/test_card_db.py
python tests/test_tactics.py
python tests/test_regression.py
python tests/test_m3_search.py
```

本地已通过：

```text
test_card_db.py: OK
test_tactics.py: OK
test_regression.py: OK
test_m3_search.py: OK
```

## 6. 服务器提交

M4 不需要 GPU。提交：

```bash
sbatch jobs/m4_carddb_tests.slurm
```

提交前替换：

```text
YOUR_USERNAME
YOUR_EMAIL
```

