# M1 V0 卡组特化规则 Agent 说明

更新时间：2026-07-06

## 1. 目标

V0 不是搜索 Agent，也不训练模型。它的职责是成为之后 M2-M5 的稳定底座：

- 不非法、不超时、不崩溃；
- 能解释核心选择；
- 搜索或模型失败时可直接退回 V0。

## 2. 模块结构

```text
submission/main.py
  -> agent/parser.py                 # 官方 observation -> ParsedState/ParsedSelect
  -> agent/rules.py                  # V0 规则打分
  -> agent/fallback.py               # 任意 select 的合法动作兜底
  -> agent/deck_profile_abomasnow.py # 卡牌常量和权重表
```

## 3. 主卡组理解

当前 deck 是 Mega Abomasnow ex 水系样例卡组：

| 卡牌 | ID | 数量 | V0 角色 |
|---|---:|---:|---|
| Basic Water Energy | 3 | 35 | 主资源，优先贴给即将攻击的 Snover / Mega Abomasnow |
| Kyogre | 721 | 2 | 次级基础 Pokémon，缺 Snover 时承压 |
| Snover | 722 | 4 | 核心基础，优先开局 Active/Bench |
| Mega Abomasnow ex | 723 | 4 | 主攻和进化目标 |
| Mega Signal | 1145 | 4 | 找 Mega Abomasnow ex |
| Maximum Belt | 1158 | 1 | 优先给主攻线 |
| Cyrano | 1205 | 2 | 找 Pokémon ex |
| Lillie's Determination | 1227 | 4 | 抽牌，6 奖赏时价值更高 |
| Waitress | 1235 | 4 | 找并贴水能 |

## 4. 规则摘要

- 开局选择：优先 Snover，其次 Kyogre；倾向先攻以拿到贴能节奏。
- Bench：优先至少保留一条 Snover -> Mega Abomasnow 进化线，不盲目铺满。
- 主阶段：进化 Mega Abomasnow ex 的优先级最高；其次给主攻线贴能；再考虑检索和抽牌。
- 检索：缺 Mega Abomasnow 时提高 Mega Signal/Cyrano 价值；缺基础时提高 Snover。
- 攻击：能 KO 时大幅加分；高伤害攻击加权；牌库过薄时降低 Hammer-lanche 的弃顶风险。
- 兜底：未知 context 或规则异常时，仍返回合法 action；可选未知选择优先少选。

## 5. 权重

所有核心分数集中在：

```text
submission/agent/deck_profile_abomasnow.py::WEIGHTS
```

调参时应优先改权重表，避免把策略结构写散。

## 6. 本地验收结果

已完成：

```text
tests/test_fallback.py      10000 个随机 select，非法动作 0
tests/test_regression.py    50 个真实 observation，全部通过
```

主要评估：

| 对局 | 输出目录 | 胜负平 | 异常 | 非法动作 |
|---|---|---:|---:|---:|
| V0 vs random, 1000 局 | `experiments/20260706_145524_submission_vs_random` | 906 / 94 / 0 | 0 | 0 / 0 |
| V0 vs 官方样例, 1000 局 | `experiments/20260706_145524_submission_vs_main` | 901 / 99 / 0 | 0 | 0 / 0 |
| V0 mirror, 1000 局 | `experiments/20260706_145524_submission_vs_submission` | 651 / 349 / 0 | 0 | 0 / 0 |
| V0 vs random, 10000 局 | `experiments/20260706_145549_submission_vs_random` | 9077 / 923 / 0 | 0 | 0 / 0 |
| V0 vs random, 1000 局，含单决策耗时 | `experiments/20260706_145738_submission_vs_random` | 919 / 81 / 0 | 0 | 0 / 0 |

10,000 局 V0 vs random：胜率 90.77%，无异常、无非法动作、无超步平局。

最新 1000 局 V0 vs random：胜率 91.9%，p95 单决策耗时 0.00019 秒。

## 7. 算力服务器

M1 仍然不需要 GPU，适合 CPU Slurm 批量评估。新增模板：

```bash
sbatch jobs/m1_cpu_eval.slurm
```

提交前替换：

```text
YOUR_USERNAME
YOUR_EMAIL
```

服务器环境仍沿用 M0：

```bash
conda create -n pokemon-tcg python=3.11 -y
conda activate pokemon-tcg
python tests/test_fallback.py
python tests/test_regression.py
python eval/run_match.py --agent0 submission --agent1 random --games 10000
```
