# Pokémon TCG AI 项目进度

> 更新时间：2026-07-29
>
> 唯一研发方案：[`docs/plan/建模方案.md`](docs/plan/建模方案.md)
>
> 详细数据清单：[`docs/plan/数据进度与待办.md`](docs/plan/数据进度与待办.md)

## 总进度

```text
███████████████░░░░░  75%
```

这里的 75% 是按研发里程碑加权的完成度，不代表已经消耗的时间比例。当前已经完成数据基础、Top10 牌表整理、稳定性测试、`SL-0-shared` 全量训练与冻结 test 评估、短序列数据、`SL-0-history` 全量对照、`SL-1-gru` 两个 seed、NumPy 在线运行时、23,400 局本地 Arena，以及 10 个 Adapter 的训练回收与统一离线复评。GRU 最终未达到晋级门槛，主线继续使用 `SL-0-shared`；补充数据正式转换和第 10 套重训复核已完成，10/10 通过离线门槛。2026-07-28 回收的 `v3_base_seed20260722_run1` 包已完成 31/31 SHA 复核和差异审计：其中 9/10 个 checkpoint 与当前冻结版逐字节相同，不能算新的独立 seed；唯一不同的 Alakazam 基础版不覆盖当前补充数据复评版。Top2 主备筛选和双分支强化尚未完成。

**当前所在位置：10 个 Adapter 已回收并完成统一复评；在线代理入口和 200 局 smoke 已完成，10/10 通过、0 异常、0 非法动作。下一步是正式 45 组循环赛与固定外部矩阵。**

```text
数据与标签 ✅
  → Top10 牌表与静态校验 ✅
  → 阶段 B 稳定性测试 ✅
  → 共享模型训练入口 ✅
  → 服务器小样本冒烟 ✅
  → SL-0 单步全量训练与冻结 ✅
  → 短序列轨迹索引与基础审计 ✅
  → padding/reset mask 与 Combo 标签 ✅
  → SL-1 GRU 服务器全量训练与首次 test ✅
  → GRU 详细复评 + 第二 seed ✅
  → SL-0-history 全量对照与三模型消融 ✅
  → GRU NumPy 在线运行时、PyTorch 一致性与代理接入 ✅
  → 固定多轮 Arena + 延迟验收 ✅ GRU 未晋级
  → Top10 采样视图与审计 ✅
  → 10 个轻量 Adapter ✅ 10/10 通过
  → Adapter 在线代理 + 200 局 smoke ✅ 10/10 通过
  → 45 组内部循环赛 + 外部矩阵 ⬜
  → 冻结 Top2 主备卡组 ⬜
  → Top2 自博弈/PPO/V1 重分析 ⬜
  → 最终提交冻结 ⬜
  → 提交后失败 replay 增量优化闭环 ⬜
```

## 分项进度

| 工作流 | 完成度 | 当前状态 | 验收依据 |
| --- | ---: | --- | --- |
| 项目结构与唯一研发主线 | 100% | 已完成 | 主方案位于 `docs/plan/建模方案.md`，旧 MVP 已归档 |
| 卡牌库、标签和基础特征 | 100% | 已完成并冻结当前规则版本 | 1,267/1,267 张通过全量规则与 schema 审计；88 张做职责级人工修正；已登记官方比赛来源、文件哈希、使用条款和 `ptcg_abc_2026_simulation_designated_pool_v1` |
| 原始回放审计与转换 | 100% | 全量完成 | 5,952 局完整 Kaggle replay 已全部转换为 841,707 个决策，0 错误 |
| 失败对局 V0/V1 数据 | 100% | 第一版完成 | 600 局、20,232 个决策；9,832 条 V0，5,000 条 V1，0 失败 |
| 正式训练集 V1 | 100% | 已按冻结标签和规则版本重建 | 861,939 个决策、6,552 局；5,000 条 V1 全部关联；无重复 ID、无跨集合对局、无非法监督动作 |
| 排行榜 Top10 候选数据 | 100% | 已完成 | 10/10 完整 60 张牌表，10/10 静态合法 |
| 阶段 B 稳定性测试 | 100% | 已完成 | 2,500 局；10/10 为 0 异常、0 非法动作 |
| 单步共享监督模型 `SL-0` | 100% | 已完成并冻结 | 6 epoch 全量训练完成；最佳 valid loss 2.1546；冻结 test loss 2.1134、policy top-1 60.27%、剔除强制单选后 57.72%、非法 top-1 为 0 |
| 短序列/Combo 模型 `SL-1` | 100% | 消融与 Arena 已结束，当前版本不晋级 | 两个 seed、history 对照、NumPy 运行时和 23,400 局 Arena 均完成；`seed1 best` finalist 胜率 50.21%、Wilson 95% 为 49.54%～50.87%，0 异常、0 非法动作、p95 23.69ms。工程合格但实战收益不足，主线保留 `SL-0-shared` |
| Top10 轻量 Adapter | 100% | 10 套训练和统一 test 复评完成，10/10 通过 | 基础 test 87,992 条；第 10 套另含 335 条按局隔离的补充 test；10/10 为 0 非法 top-1 |
| 内部循环赛与外部矩阵 | 10% | 在线代理与 200 局 smoke 已通过，正式矩阵未开始 | 10/10 候选为 0 异常、0 非法动作；仍需 45 个内部组合、交换先后手及固定外部对手 |
| Top2 卡组强化与搜索 | 0% | 等待 Top2 | 主卡组与备选卡组确定后，分别运行受预算约束的 masked PPO、自博弈和新增 V1 重分析 |
| 最终学习模型提交 | 0% | 未晋级 | 当前仍由 Flat Safe V0 作为历史安全基线 |
| 提交后 replay 增量优化 | 0% | 方案已写入，尚未实施 | 按提交版本下载、重分析、小步更新、回归和 Arena 晋级 |

## 已完成的关键成果

- 第一版正式训练集已经落盘：`data/training/training_decisions_v1.jsonl`。
- 训练集 manifest 审计通过：861,939 个唯一决策，训练/验证/测试按整局隔离。
- Top10 已全部生成 `deck.csv`、映射报告、静态合法性报告和回放先验。
- 阶段 B 已实际完成 2,500 局，所有候选均通过稳定性硬门槛。
- 当前回放先验和阶段 B 的领先者是 `Crustle / Mega Kangaskhan ex - Cage`，但它还不是已冻结的 Top2。
- 现有 `submission/deck.csv` 是 Abomasnow 旧基线，不代表 Top10 最终结果。
- 训练方案已固定为“一个共享主干 + 十个轻量 Adapter”，不训练十个独立大模型。
- `SL-0-shared` 完整产物已回收至 `artifacts/sl0_shared_full/`，冻结评估报告为 `reports/sl0_shared_test.json`。
- `SL-1-gru` 完整 checkpoint 已回收至 `artifacts/sl1_gru_full/`，首次冻结评估报告为 `reports/sl1_gru_test.json`；`best.pt` 为 epoch 1，`last.pt` 为 epoch 5。
- `SL-0-history` checkpoint、训练配置和逐 epoch 指标已回收至 `artifacts/sl0_history_full/`；其 policy 优于 SL-0，但 value 与总 loss 退化，证明简单显式历史不足以替代 GRU。
- 第二 seed GRU checkpoint 已回收至 `artifacts/sl1_gru_seed20260721/`；详细 test 中总 loss 2.0294、policy top-1 63.44%、非强制 top-1 61.10%、非法 top-1 为 0，policy 改善方向得到复现。
- GRU NumPy 在线运行时、长度 16 滚动窗口、跨局 reset、合法动作回退和独立提交包已实现；PyTorch/NumPy 最大 logit 误差为 0.00124，动作一致。
- 本地交换先后手 Arena 已完成两轮各 200 局初筛：首轮 GRU 58%，次轮 49%；次轮 Wilson 95% 区间为 42.16%～55.88%，0 异常、0 非法动作、p95 决策时延 23.54ms、模型动作覆盖率 74.71%。底层比赛库未暴露 RNG seed，命令行 seed 只能固定 Python 侧代理，故两轮应视为独立重复而非严格配对 seed；结果尚不稳定，不能晋级。
- GRU checkpoint 淘汰赛与大样本复验共完成 23,400 局；`seed1 best` 在 21,800 局中胜率 50.21%、Wilson 95% 为 49.54%～50.87%，未达到超过 2 个百分点的晋级幅度。最终冻结 `SL-0-shared` 作为 Adapter 共用主干。
- Top10 Adapter 采样 manifest 已生成至 `data/adapter_views/`：基础训练集 861,939 条，加上 `alakazam_battle_cage_split` 的 7,494 条定向轨迹后共审计 869,433 条；补充数据含 train 6,386、valid 773、test 335，0 个跨 split 对局，10/10 视图全部通过。
- 10 个 Adapter 训练包已校验并解压至 `artifacts/adapters_top10/`；回传包 SHA-256 为 `66702A8C85B1FFDDBB29293A33E750F023F671CC46B0B62800B7A9A4AF70FCB7`，10/10 checkpoint 可加载且绑定冻结主干数据哈希。
- 统一离线复评已完成，详见 `reports/top10_adapter_offline_eval.md` 和 `.json`：10/10 为 0 非法 top-1，10/10 通过离线门槛。
- 2026-07-28 回收的 `top10-adapters-v3-base-seed20260722-run1.tar.gz` 外层哈希和包内 31 项 SHA 均通过；10/10 离线 `advance`、非法预测 0、batch=1 p95 1.651～1.664ms。逐文件比较发现 9/10 与当前冻结 checkpoint 完全相同，故不作为独立 seed 或替换依据；详见 `reports/top10_adapter_v3_base_seed20260722_run1_review.md`。
- 已实现 `adapter:<candidate_id>` 在线代理入口和统一 smoke 脚本；Random + mirror 共 200 局，10/10 通过、0 异常、0 非法动作、8,921 次模型决策，候选最大 p95 单决策耗时 2.82ms。机器可读结果见 `reports/top10_adapter_online_smoke.json`。
- 7,494 条定向补充轨迹已转换为正式监督增量：100 局、0 重复、0 非法监督动作、0 跨 split；RTX 5060 重训 4 epoch 用时 24 分 23 秒。完整 452 条 exact test 上 `alakazam_battle_cage_split` Top-1 相对主干提升 17.37pp，batch=1 p95 2.79ms。

## 未完成事项

### P0：现在必须完成

- [x] 已实现能够流式读取 `training_decision_v1`、支持 gzip、worker/rank 分片和有限 shuffle buffer 的 DataLoader。
- [x] 已实现共享 State/Option/Deck Encoder、动态合法 option 打分头及加权 policy/value loss。
- [x] 已在服务器 GPU 完成冒烟和 6 epoch 全量训练，回收 best/last checkpoint、配置和全部 epoch metrics。
- [x] 已固定 `SL-0` 模型配置、特征 schema、随机种子、数据哈希和冻结 test 指标。
- [x] 按 `game_id/player/turn/decision_index` 审计并构造短决策序列，保证无未来信息和隐藏信息泄漏。
- [x] 生成 Combo 前置条件、未来 N 步进化/贴能/攻击/击倒等可验证弱标签。
- [x] 完成小型 `SL-1-gru` 的首次服务器全量训练和冻结 test。
- [x] 用新版评估器对 GRU `best.pt`/`last.pt` 补齐非强制单选、来源分组、非法 top-1 和推理吞吐，并用第二 seed 复跑。
- [x] 完成 `SL-0-history` 服务器全量训练，比较单步、显式历史和 GRU 三组消融。
- [x] 已以合法动作、固定 Arena 胜率、推理 p95 和回退率完成序列主干决策；GRU 因胜率收益不足不晋级，故不再追加阻塞主线的 Combo 验收。
- [x] Top10 采样视图已全部生成并区分 exact/similar/general；定向补充 100 局后 10/10 通过，且未移动原 valid/test 样本。
- [x] 冻结共享主干，训练并回收 10 个轻量 Deck Adapter 和卡组条件化策略头。
- [x] 统一检查 10 个 Adapter 的非法 top-1、离线损失和推理时间；10/10 通过。
- [x] 将 `alakazam_battle_cage_split` 补充轨迹转换为正式监督 schema，重训并完成包含补充 test 的复评。

### P0：Top2 主备选择

- [x] 完成 10 套 Adapter 在线代理接入、牌表/checkpoint 绑定、安全回退和每套 20 局 smoke。
- [ ] 运行 10 套完整单循环，共 45 个内部对战组合。
- [ ] 每个组合使用共同 seed，并交换先后手。
- [ ] 同时对 Random、Sample、Exploiter-FirstMin、V0-best 和稳定历史模型运行外部矩阵。
- [ ] 按 `50% 内部循环赛 + 30% 外部基线 + 10% 最差对局 + 10% 稳定性/时延` 计算综合分。
- [ ] 前四复赛、前二决赛，并检查循环克制和置信区间。
- [ ] 冻结最终 Top2：第 1 名为主卡组、第 2 名为备选卡组；分别保存牌表、Adapter、提交包和报告哈希。
- [ ] 只有主卡组通过最终发布门槛时才替换 `submission/deck.csv`；备选卡组保留独立提交包，不覆盖主包。

### P1：Top2 确定后

- [ ] 为主卡组与备选卡组分别补充专属自比赛轨迹和 V0 老师数据，严格保留 `deck_id`。
- [ ] 从两套卡组各自的低置信、失败和胜负转折局面生成新一轮 V1 标签。
- [ ] 对 Top2 两个分支分别运行 masked PPO，混入监督数据并使用 KL 约束防止遗忘；主卡组获得主要预算，备选卡组使用较小但完整的保底预算。
- [ ] 建立 best/candidate/history 对手池和自动 Arena 晋级流程。
- [ ] 运行 shadow mode、V0 回退、时延和完整对手矩阵验收。
- [ ] 通过全部门槛后构建主、备两个独立学习模型提交包；正式提交默认使用主包，主包回归失败时可切换备选包，否则继续使用 Flat Safe V0。
- [ ] 为每次正式提交登记 `submission_id`、牌表、模型、Adapter、代码和 schema 哈希。
- [ ] 按提交版本下载我方完整 replay，识别我方玩家并生成 post-submission 数据版本。
- [ ] 冻结 20% 新失败局作为永不训练的版本回归集。
- [ ] 对输局关键状态运行 V1 反事实重分析，禁止直接把失败动作当高权重老师标签。
- [ ] 以新输局重标、同版本赢局和历史数据混合方式分别小步更新 Top2 Adapter，禁止跨牌组混淆版本。
- [ ] 只有新旧回归集和完整 Arena 同时通过，才发布下一版；否则回滚。

### P2：持续补充但不阻塞当前训练

- [ ] 获取并复核更多真实人类完整 replay；当前正式高手训练集仍不足。
- [ ] 补齐赛事牌表的日期、名次、规则版本和来源许可。
- [x] 卡牌库已补充官方比赛来源 URL、抓取日期、文件哈希和 `ptcg_abc_2026_simulation_designated_pool_v1`。
- [x] 已按 [`docs/operations/卡牌标签人工复核操作指南.md`](docs/operations/卡牌标签人工复核操作指南.md) 完成 1,267 张全量规则审计，并对当前提交和 Top10 的 88 张卡完成职责级人工修正；详见 [`卡牌标签人工复核记录.md`](docs/operations/卡牌标签人工复核记录.md)。
- [ ] 固定永不进入训练的回归评估集并保存哈希。

## 当前数据口径

| 数据 | 数量 | 注意事项 |
| --- | ---: | --- |
| 正式训练集 | 861,939 个决策 / 6,552 局 | 已通过自动审计；包含全量 Kaggle 转换数据和 600 局失败对局 |
| Kaggle 转换数据 | 841,707 个决策 / 5,952 局 | 已全量转换并合入正式训练集；是模拟智能体行为，不是真实人类高手标签 |
| 失败对局 | 20,232 个决策 / 600 局 | V0/V1 来自 Abomasnow 旧基线，可用于跨卡组监督 |
| V1 搜索标签 | 5,000 条 | 旧基线标签，Top2 确定后需要新增两套卡组各自的专属版本 |
| Top10 候选 | 10 套 | 10 套已训练并通过离线门槛，可进入 Arena |
| 阶段 B | 2,500 局 | 只证明稳定性和提供运行优先级，不能直接决定 Top2 |

## 训练与评估时间预估

> 以下均为墙钟时间粗估，不是承诺工期。服务器 GPU、CPU 核数、NVMe 吞吐、混合精度、batch size 和并行对局数尚未实测，第一次冒烟测试后必须用实际吞吐重算。时间不包含排查严重 bug、重新设计模型或服务器排队等待。

| 阶段 | 服务器粗估 | 本地 RTX 5060 粗估 | 主要影响因素 |
| --- | ---: | ---: | --- |
| 训练入口、DataLoader 和模型实现 | 3～8 小时人工开发 | 相同 | 不是 GPU 训练时间；动态 option 与流式读取需要先验证 |
| 数据上传、解压与完整性校验 | 15 分钟～2 小时 | 10～40 分钟 | 网络速度、磁盘顺序读写；Release 压缩包约 306 MiB，解压后约 10 GiB |
| 1,000～10,000 条小样本冒烟测试 | 10～40 分钟 | 20～60 分钟 | 首次编译、显存、batch size、DataLoader 吞吐 |
| `SL-0-shared` 全量单步监督训练 | 1～6 小时 | 3～10 小时 | epoch 数、早停、混合精度、861,939 条样本吞吐 |
| 短序列构造、`SL-1` 训练与消融 | 待 `SL-0` 吞吐后估算 | 待实测 | 窗口长度、GRU 隐层、序列 I/O、Combo 标签和 Arena 对局量 |
| 冻结主干并训练 10 个 Adapter | 1～5 小时 | 3～10 小时 | 是否并行、每个 Adapter 的采样量和早停 |
| 45 组内部循环赛 | 2～8 小时 | 4～12 小时 | 主要受 CPU 模拟并行度和每组对局数影响 |
| 外部矩阵、前四复赛和前二决赛 | 2～8 小时 | 4～12 小时 | 对手数量、置信区间、是否需要追加对局 |
| Top2 自博弈、V1 重分析和 masked PPO | 约 5～18 小时 | 约 9～36 小时 | 主分支使用主要预算，备选分支使用约 30%～50% 保底预算 |
| 最终回归、打包和提交检查 | 1～3 小时 | 1～4 小时 | 回归矩阵、推理时延和 Kaggle 包验证 |

### 总体判断

- 原“共享模型 + 10 个 Adapter”服务器 **5～15 小时**、Top2 冻结前本地 **14～40 小时**的估算只覆盖单步模型，不包含新增序列数据、`SL-1` 训练和消融，不能再作为完整总工期。
- `SL-0` 实测每个 epoch 约 290～293 秒，6 epoch 约 29 分钟；`SL-1` 必须根据窗口数量、序列吞吐和 Arena 对局量另行实测。
- Top2 筛选、双分支强化与最终回归的总工期，要按实测循环赛吞吐重新计算。

### 第一次服务器冒烟测试必须记录

- GPU 型号、显存、CPU 核数、内存和磁盘类型；
- 每秒训练样本数、单 epoch 时间、峰值显存和 GPU 利用率；
- JSONL 解码/DataLoader 等待占比；
- 单局模拟耗时、可安全并行的 worker 数；
- 据此重新填写共享训练、Adapter 和循环赛的实测预计完成时间。

## 下一步唯一动作

实现正式 Arena 统一编排并启动 45 组内部循环赛，使用共同对局设置并交换先后手，同时运行固定外部对手矩阵。`alakazam_battle_cage_split` 的基础来源 exact 子集与完整 exact test 方向不同，Arena 中需重点检查跨来源泛化。
