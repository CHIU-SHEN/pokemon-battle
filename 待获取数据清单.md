# Pokémon TCG AI 数据现状与待办清单

> 本文先回答三个问题：**我们已经有什么、现在能直接加工什么、还必须从外部获取什么**。
> 审计时间：2026-07-13。可重复运行 `scripts/audit_existing_data.py` 更新统计。

## 1. 一眼看懂当前状态

| 状态 | 数据 | 当前数量 | 能否直接用于训练 | 下一步 |
| --- | --- | ---: | --- | --- |
| 已完成 | 卡牌基础库 | 1,267 张 | 可用于卡牌特征 | 定期核对规则版本和来源 |
| 已完成 | 卡牌功能标签 | 1,267 张 | 可用于卡牌/动作特征 | 人工复核目标卡组标签 |
| 已完成 | 正式目标卡组 | 60 张 | 可用于卡组特征 | 已可生成统一 profile 和哈希 |
| 已有、可加工 | 精英卡组 | 46 副 | 可做候选卡组与模拟对手 | 校验合法性、算相似度 |
| 已有、可加工 | 失败对局 | 600 局、20,232 个决策 | 可训练实际动作/value；暂不可当搜索老师真值 | 转换、去重、分类、补老师标签 |
| 已有、仅评估 | 模拟比赛结果 | 60 个结果文件、约 34,146 局 | 可做胜率评估，不等于 policy 轨迹 | 与训练集严格隔离 |
| 已有、仅冒烟 | 蒸馏数据 | 5 局、50 条 | 只能验证代码链路 | 扩大正式采集 |
| 已有 | 重分析队列 | 已生成 | 可驱动搜索补标 | 扩展到失败日志 |
| 获取中 | 外部完整 replay | 已获取 1 局 PTCGL 社区样例（12 回合，待事件级复核）；真实高手正式集仍为 0 | 复核后可验证解析链路，不足以训练高手 policy | 继续获取 Kaggle 官方 replay 与获授权 Battle Log |
| 待外部获取 | 赛事牌表元数据 | 文档中已有部分人工汇总 | 可训练卡组识别/环境分布 | 结构化并保存来源许可 |

结论：当前最缺的不是“更多牌表”，而是**真实高手的逐步对局轨迹**和**对现有困难局面的高质量搜索老师标签**。

## 2. 已直接处理的内容

运行：

```powershell
python scripts/audit_existing_data.py
```

脚本会生成以下可追溯产物：

| 产物 | 用途 |
| --- | --- |
| `data/processed/local_data_audit.json` | 全仓库数据数量、覆盖率、可用性审计 |
| `data/processed/target_deck_profile.json` | 目标卡组卡名、数量、类型、HP、标签、卡组哈希 |
| `data/processed/bad_case_index.json` | 600 个失败案例的路径、对局、步数、原因索引 |

注意：当前 Windows 环境中的 `python.exe` 是无可用解释器的系统别名，因此脚本已写好，但本次无法在该环境落盘生成三个 JSON；安装/启用 Python 后即可直接运行。原始统计已通过 PowerShell交叉核对。

## 3. 现有数据可以直接做什么

> 当前已决定优先使用 Kaggle 高分回放中的 10 副去重卡组作为候选池。具体筛选、策略适配、复赛和最终冻结规则见 `高分回放卡组筛选框架.md`，机器可读候选清单见 `data/high_score_deck_selection.json`。

### A. 目标卡组与卡牌数据（现在即可做）

- [x] 检查目标卡组是否为 60 张。
- [x] 将卡牌 ID 关联到名称、类型、HP 和功能标签。
- [x] 为目标卡组生成稳定哈希，追踪版本变化。
- [ ] 人工复核主攻手、辅助、检索、抽牌、回收、换位、能量加速等关键标签。
- [ ] 给卡牌库补充 `source_url`、`retrieved_at`、`ruleset_version`；这些信息无法从现有文件可靠推断。

验收：目标卡组每个 ID 在 `cards.json` 和 `card_tags.json` 中都有唯一记录；总数为 60；同名版本不错误合并。

### B. 600 个失败日志（现在可转换，之后再搜索补标）

现有日志每局包含 `trace`，每步已有行动玩家、可见 observation、选项、实际动作和最终结果。因此可直接派生：

- `game_id / decision_id / player / turn`
- 可见局面特征、合法选项、实际动作
- 最终胜负、动作耗时、对局双方策略名称
- 公开日志序列和基础困难样本标签

暂时不能可靠派生：

- V1 搜索的候选访问量、Q 值和风险分位数
- 真实高手动作标签
- 双方真实牌表 ID（日志若未明确保存，不应猜测）
- 精细失败原因（如“错过击倒”）——需要规则检测器或搜索反事实验证

处理要求：

- [ ] 按 `case_id` 去重，保留原文件路径。
- [ ] 每步只使用行动者可见的 observation，禁止解码或加入隐藏全状态。
- [ ] 唯一动作样本单独标记，训练时降采样。
- [ ] 先输出 `observed_action` 与 `game_result`；没有搜索时，老师字段置空，不能拿实际动作冒充老师动作。
- [ ] 对关键/分歧局面运行 V1 重分析后，再补 `search_action/q_values/visits/risk`。

### C. 34,146 局实验结果（现在可汇总，但只用于评估）

这些文件适合计算胜率、置信区间、先后手差异、对手分层结果、非法动作率和耗时；多数没有足够的逐步局面信息，不能据此训练 policy。

- [ ] 建立统一评估表：agent、deck、opponent、seed、先后手、result、steps、latency。
- [ ] 按相同 seed 成对比较候选方案。
- [ ] 固定一部分文件为永不进入训练的回归评估集，并保存文件哈希。

### D. Top10、高分回放牌表和 46 副精英卡组

- [ ] 解析成统一 deck schema：`deck_id/cards/source/event/date/placement/ruleset`。
- [ ] 校验 60 张、卡牌 ID、同名上限及规则合法性。
- [ ] 计算卡牌数量向量、体系标签、与目标卡组的 Jaccard/余弦相似度。
- [ ] 用作模拟对手池或卡组条件特征。

只有牌表和排名不能生成逐步动作标签，也不能计入“完整 replay 局数”。

## 4. 真正需要外部获取的数据

### P0：真实高手结构化对局

当前获取进度（2026-07-13）：已下载并登记 1 局完整 PTCG Live 社区样例及其 12 回合 JSON；原始日志、摘要 JSON、逐回合 JSON 是同一局，不重复计数。另已获取 MIT 许可的 PTCG-Bench 轨迹生成环境，但仓库不含预生成对局，计 0 局。详见 `data/external/acquisition_manifest.json`。

每局至少需要：双方牌表、事件顺序、每次选择、最终结果、规则版本和来源许可。优先级为：官方 replay/API > 平台可下载 battle log > 可人工还原录像 > 文字战报。

- [ ] 第一阶段：100 局，用来验证解析、可见性和动作映射（当前 1/100；该局待事件级复核）。
- [ ] 第一版：1,000 局跨卡组完整对局。
- [ ] 能重建合法动作的样本训练 policy；不能重建的只训练 value、意图或事件序列。
- [ ] 保存 `source_url`、许可说明、抓取时间、解析器版本和原始文件哈希。

### P0：赛事牌表与环境元数据

- [ ] 目标卡组常见变体。
- [ ] Alakazam、Crustle、Dragapult、Marnie、Garchomp、Slowking、Basic Box 等主要体系。
- [ ] 每副牌保存赛事、日期、名次、规则版本和完整 60 张。
- [ ] 第一版目标为目标卡组加 5～20 副高质量常见/相似牌表；不要单纯追求数量。

### P1：官方卡牌来源与规则版本

- [ ] 为现有 1,267 张卡补来源 URL、抓取日期、规则版本。
- [ ] 核对攻击、效果、费用、进化关系和同名不同版本。
- [ ] 记录 API/页面许可与限速要求；公开可浏览不代表允许无限抓取。

## 5. 正式训练样本的最低 schema

每条逐步样本必须包含：

| 字段组 | 必填内容 | 缺失时怎么处理 |
| --- | --- | --- |
| 身份 | schema、game_id、decision_id、player、turn、ruleset | 不进正式集 |
| 输入 | 行动者可见状态、截至当前的公开事件 | 不可补未来信息 |
| 动作 | 全部合法动作、稳定动作 ID、实际动作 | 无合法动作集合时不训练 policy |
| 老师 | V0 动作；V1 候选分数/访问量/排名 | 没跑老师就置空 |
| 结果 | 最终胜负、value、risk | 只训练实际存在的目标 |
| 卡组 | 双方 deck_id、特征、相似度 | 未知就置空，不猜测 |
| 来源 | source、quality、weight、生成器版本 | 无来源不进入正式集 |

同一 `game_id` 必须整体进入 train、valid 或 test 中的一个集合，禁止按决策随机切分。

## 6. 推荐执行顺序

1. 安装/启用 Python，运行本地审计脚本并提交三个 `data/processed` 产物。
2. 把 600 个失败日志转换为“可见状态 + 合法动作 + 实际动作 + 结果”的基础样本。
3. 对基础样本做 schema 校验、隐藏信息检查、去重和按局划分。
4. 将 Top10/高分牌表和精英卡组结构化，建立带来源的对手池。
5. 修改采集器，让新模拟局默认保存完整逐步轨迹及 deck_id。
6. 从失败、低置信度和 V0/V1 分歧局面生成重分析队列并补搜索标签。
7. 外部获取 100 局真实结构化 replay，先打通解析和许可审计。
8. 达到至少 100,000 条有效监督决策后训练第一版共享模型。
9. 固定评估集和哈希，再做目标卡组自博弈微调。

## 7. 第一版验收门槛

- [x] 卡牌库与标签库已有基础覆盖。
- [x] 目标卡组和失败日志具备直接加工条件。
- [ ] 目标卡组关键功能标签人工复核完毕。
- [ ] 600 个失败日志完成统一转换，老师缺失字段不造假。
- [ ] 目标卡组加 5～20 副常见/相似卡组完成标准化。
- [ ] 至少 1,000 局完整轨迹、100,000 条有效决策。
- [ ] 至少 50,000 条 V0 老师样本和 5,000 条 V1 关键决策。
- [ ] 固定评估集隔离并保存哈希。
- [ ] 审计无隐藏信息泄漏、同局跨集合、非法卡组或动作 ID 不一致。

最重要的口径：**“局数统计”“牌表”“逐步轨迹”“老师标注”是四种不同数据，不能互相替代。**
