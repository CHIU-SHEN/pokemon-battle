# Pokémon TCG AI 待获取数据清单

> 目标：为“监督蒸馏 → 目标卡组适配 → 轻量自博弈 → 关键局面搜索”的建模路线准备可训练、可追溯、无信息泄漏的数据。
>
> 核心原则：牌表和比赛结果不能替代逐步对局轨迹；外部真实对局负责提供高手行为和环境覆盖，模拟器负责批量生成格式完整的数据。

## 1. 当前数据基线

| 已有内容 | 位置 | 当前判断 |
| --- | --- | --- |
| 卡牌基础资料 | `data/cards.json` | 已有基础，需要检查规则版本、效果和覆盖率 |
| 卡牌功能标签 | `data/card_tags.json` | 已有基础，需要补齐目标及跨卡组标签 |
| 正式目标卡组 | `submission/deck.csv` | 需要生成卡组 ID、版本和统一特征 |
| 候选与精英卡组 | `data/deck_candidates.json`、`data/deck_elites/` | 可作为相似卡组和模拟对手来源 |
| Top10 与高分回放牌表 | `实际比赛Top10与Kaggle高分回放卡组汇总.md` | 可用于卡组库，不能直接替代逐步训练样本 |
| 蒸馏冒烟数据 | `data/distill/smoke_samples.jsonl` | 仅 5 局、50 条样本，只能验证流程 |
| 失败对局 | `logs/bad_cases/` | 约 600 个文件，优先转换为统一样本 |
| 重分析入口 | `data/reanalysis_queue.json` | 已有入口，需要持续填充和生成搜索标签 |
| 模拟评估记录 | `experiments/`、`data/deck_eval_records.json` | 可用于评估，但需确认是否保存完整逐步轨迹 |

当前主要缺口不是牌表数量，而是包含“可见状态、合法动作、实际选择、老师标签、公开历史和最终结果”的逐步决策样本。

## 2. P0：训练前必须完成的数据

### 2.1 统一逐步决策样本

- [ ] 固定 schema 版本，并在每条样本中保存：
  - [ ] `game_id`、`turn_id`、`decision_id`、行动玩家和规则版本
  - [ ] 当前玩家真正可见的完整状态
  - [ ] 当前全部合法动作及稳定动作 ID
  - [ ] 玩家实际动作
  - [ ] V0 规则老师动作
  - [ ] V1 搜索老师的候选分数、概率或排名
  - [ ] 最终胜负、局面 value 和 risk 标签
  - [ ] 双方 `deck_id`、卡组特征和卡组相似度
  - [ ] 截至当前的对手公开事件历史
  - [ ] 数据来源、来源质量、样本权重和生成程序版本
- [ ] 确保隐藏手牌、牌库顺序和奖赏卡不会泄漏到模型输入。
- [ ] 按完整 `game_id` 划分训练、验证和测试集，禁止同一局跨集合。

### 2.2 卡牌资料和语义标签

- [ ] 检查目标卡组全部卡牌是否存在于 `cards.json`。
- [ ] 补齐名称、卡牌 ID、规则版本、类型、HP、费用、攻击、效果和进化关系。
- [ ] 为卡牌标注主攻、辅助、抽牌、检索、进化、回收、换位、控制和能量加速等功能。
- [ ] 对官网卡牌资料建立来源 URL、抓取日期和规则版本记录。
- [ ] 对同名不同版本卡牌保留不同内部 ID，不按名称错误合并。

### 2.3 卡组库

- [ ] 收集目标卡组及其常见变体。
- [ ] 收集机制相似卡组。
- [ ] 收集当前环境主要对手卡组。
- [ ] 收集快攻、进化、控制、耐久、回收、能量加速和工具箱等不同类型卡组。
- [ ] 每副牌保存来源、赛事、日期、名次、规则版本和完整 60 张牌。
- [ ] 统一卡名和卡牌 ID，并校验总张数、同名上限与合法性。
- [ ] 计算卡组特征、体系标签和与目标卡组的相似度。

第一版最低目标：目标卡组加 5～20 副常见或相似卡组。优先覆盖 Alakazam、Crustle、Dragapult、Marnie、Garchomp、Slowking、Basic Box 等已有资料中的体系。

### 2.4 完整真实对局轨迹

- [ ] 优先寻找官方 replay、官方 API、平台 battle log 或可下载 JSON。
- [ ] 每局至少取得双方牌表、事件顺序、每次选择、最终结果和规则版本。
- [ ] 能还原合法动作时生成 policy 样本；不能还原时只用于价值、意图或事件序列训练。
- [ ] 同一录像交替从双方视角生成样本。
- [ ] 对数据来源进行许可检查，遵守服务条款、API 限速和再利用要求。

第一版目标：至少 1,000 局跨卡组完整对局。只有牌表、排名或胜负摘要的网页不能计入这个数量。

### 2.5 目标卡组专属轨迹

- [ ] 目标卡组对 V0 规则策略。
- [ ] 目标卡组对 V1 搜索策略。
- [ ] 目标卡组对当前 best 和历史 checkpoint。
- [ ] 目标卡组对主流不同卡组。
- [ ] 目标卡组镜像对局。
- [ ] 使用成对随机种子交换先后手，降低牌序噪声。
- [ ] 保存完整决策轨迹，而不仅是最终胜负。

第一版目标：至少 500 局、50,000 条目标卡组有效决策样本。

## 3. P1：第一版模型前强烈建议完成

### 3.1 规则老师数据

- [ ] 用 V0 批量生成合法、稳定的基础对局。
- [ ] 覆盖不同卡组、先后手和随机种子。
- [ ] 保存每一步全部合法动作和规则老师选择。
- [ ] 检测重复状态，避免大量简单或唯一动作淹没训练集。

目标：至少 50,000 条有效决策。

### 3.2 搜索老师与关键局面数据

- [ ] 从失败、低置信度和模型分歧局面中建立重分析队列。
- [ ] 优先收集击倒临界、进化分歧、贴能目标、撤退换位、薄牌库和奖赏卡临界局面。
- [ ] 对 top-k 合法动作保存搜索访问量、均值、低分位风险和最终排序。
- [ ] 保存搜索预算、belief 采样数、随机种子和搜索版本。

目标：第一版覆盖 5,000～20,000 个关键决策。

### 3.3 失败与边界局面

- [ ] 将 `logs/bad_cases/` 转换成统一逐步样本。
- [ ] 标记失败原因：非法风险、资源浪费、错过击倒、进化失败、断能、牌库耗尽等。
- [ ] 去重并按失败类型分层采样。
- [ ] 无法完整还原合法动作的文件只训练适用的辅助目标。

目标：至少 1,000 个有效困难局面。

### 3.4 对手公开行为序列

- [ ] 保存对手打出的卡、贴能对象、进化、撤退、换位、攻击和公开检索结果。
- [ ] 保存手牌数、牌库数、奖赏卡数和场面资源的公开变化。
- [ ] 给公开事件加回合、阶段、行动顺序和卡牌 ID。
- [ ] 从未来公开行为生成“下一意图、下一动作类型、关键威胁”标签。

## 4. P2：强化学习和稳定迭代前完成

### 4.1 多样化自博弈对手池

- [ ] V0、V1、当前 best 和多个历史 checkpoint。
- [ ] 激进、保守、控制、资源积累等规则变体。
- [ ] 不同主流卡组和镜像对手。
- [ ] 记录对手策略版本，限制单一对手占比。

目标：每轮生成 2,000～10,000 局、200,000～1,000,000 条决策样本。

### 4.2 永不进入训练的固定评估集

- [ ] 固定卡组、对手、种子、先后手和关键局面。
- [ ] 每个主要对手至少数百局。
- [ ] 记录胜率及置信区间、非法动作率、决策延迟和搜索触发率。
- [ ] 单独评估不同动作类别，防止局部能力退化。
- [ ] 数据清单和哈希固定，禁止混入训练或重分析。

## 5. 外部数据来源及用途

| 外部数据 | 可以训练什么 | 不能直接训练什么 | 操作方式 |
| --- | --- | --- | --- |
| 官方卡牌数据库 | 卡牌语义、费用、进化关系 | 局面动作策略 | 优先用官方 API/下载；保存版本和来源 |
| 官方赛事牌表、排名 | 卡组库、环境分布、卡组识别 | 逐步动作 policy | 解析为标准 60 张牌表并做合法性校验 |
| 官方/平台结构化 replay | policy、value、对手建模 | 不可见信息推断的真值输入 | 按双方可见视角重建逐步样本 |
| 官方直播和录像 | 关键高手选择、战术意图 | 大规模精确合法动作监督 | 人工标少量关键局面，标注不确定性 |
| 文字战报 | 战术标签、对局背景 | 完整逐步策略 | 作为弱标签或检索资料，不直接混入高权重 policy 数据 |
| 本地模拟器 | 大规模完整轨迹 | 真实高手分布 | 用真实牌表建对手池，由规则/搜索老师标注 |

官网数据可以使用，但获取前必须记录许可情况。公开可浏览不等于允许无限抓取；优先选择官方 API、下载入口或明确允许研究使用的数据。

## 6. 推荐数据配比

第一版训练批次可从以下比例起步：

| 数据类别 | 建议占比 |
| --- | ---: |
| 目标卡组及其变体 | 40%～50% |
| 机制相似卡组 | 20%～25% |
| 当前主流对手卡组 | 20%～25% |
| 其他不同类型卡组 | 10%～15% |

完全不同卡组的数据主要训练局面价值、资源管理、战术意图和对手预测；不要让它高权重监督目标卡组的具体动作。

## 7. 实际执行顺序

1. 冻结统一 schema、可见性规则和 ID 体系。
2. 审计 `cards.json`、`card_tags.json` 和目标卡组覆盖率。
3. 将现有约 600 个失败日志转换、去重并分类。
4. 修改 V0/V1 采集器，使其保存完整合法动作和公开事件。
5. 将已有 Top10 与高分牌表导入统一卡组库。
6. 从允许使用的官网/API 获取更多牌表、赛事元数据和结构化 replay。
7. 使用真实牌表在模拟器中生成跨卡组和目标卡组轨迹。
8. 对失败、低置信度和分歧局面运行 V1 重分析。
9. 达到 10 万条监督决策后训练第一版共享模型。
10. 建立固定评估集，再开始目标卡组自博弈微调。

## 8. 数据验收标准

随机抽取任意训练样本，必须能回答：

- [ ] 这是哪局、哪回合、谁在行动？
- [ ] 行动者当时真正能看到什么？
- [ ] 当时有哪些合法动作？
- [ ] 实际选择和老师推荐分别是什么？
- [ ] 双方使用什么卡组，属于什么体系？
- [ ] 对手此前公开做过什么？
- [ ] 最终谁赢，当前局面的 value/risk 标签是什么？
- [ ] 数据来自哪里，规则和生成程序是什么版本？
- [ ] 该样本应该使用什么训练权重？
- [ ] 是否与验证集或测试集中的同一对局重复？

只要其中任一项无法回答，就应降低样本权重、屏蔽不可靠训练目标，或暂不进入正式训练集。

## 9. 第一版里程碑

- [ ] 目标卡组全部卡牌资料和功能标签完整。
- [ ] 目标卡组加 5～20 副常见/相似卡组完成标准化。
- [ ] 1,000～5,000 局完整对局。
- [ ] 100,000～500,000 条有效监督决策样本。
- [ ] 至少 50,000 条规则老师样本。
- [ ] 5,000～20,000 条搜索老师关键决策。
- [ ] 至少 1,000 个失败或困难局面。
- [ ] 固定评估集完成隔离并生成数据哈希。
- [ ] 数据审计报告无隐藏信息泄漏、同局跨集合或动作 ID 不一致。

