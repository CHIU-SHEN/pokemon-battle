# M0 交付说明：环境、数据和基准冻结

更新时间：2026-07-06

## 1. 已完成内容

M0 已按 `执行文档.md` 完成：

- 保留并修正官方提交骨架：`submission/`
- 支持 `agent(None)` 返回 60 张卡组
- 新增本地评估器：`eval/run_match.py`
- 新增最小回归测试：`tests/test_regression.py`
- 采集 50 条 observation fixture：`tests/fixtures/observations.json`
- 新增 Slurm 模板：
  - `jobs/m0_cpu_eval.slurm`
  - `jobs/m3_gpu_train_template.slurm`

## 2. 本地验收结果

已在本机完成 3 组 1000 局评估：

| 对局 | 输出目录 | 胜负平 | 异常 | 非法动作 | p95 agent 耗时/局 |
|---|---|---:|---:|---:|---:|
| submission vs random | `experiments/20260706_144342_submission_vs_random` | 526 / 474 / 0 | 0 | 0 / 0 | 0.0081s |
| random vs random | `experiments/20260706_144410_random_vs_random` | 487 / 513 / 0 | 0 | 0 / 0 | 0.0070s |
| submission mirror | `experiments/20260706_144410_submission_vs_submission` | 499 / 501 / 0 | 0 | 0 / 0 | 0.0094s |

回归测试：

```bash
python3 tests/test_regression.py
# OK: 50 fixtures passed
```

## 3. 常用命令

从项目根目录执行：

```bash
python3 eval/run_match.py --agent0 submission --agent1 random --games 1000 --save-fixtures 50
python3 eval/run_match.py --agent0 random --agent1 random --games 1000
python3 eval/run_match.py --agent0 submission --agent1 submission --mirror --games 1000
python3 tests/test_regression.py
```

评估结果会写到：

```text
experiments/YYYYMMDD_HHMMSS_<agent0>_vs_<agent1>/summary.json
experiments/YYYYMMDD_HHMMSS_<agent0>_vs_<agent1>/games.json
```

## 4. 哪些内容需要提交到算力服务器

M0 只需要 CPU，不需要 GPU。建议把下面内容上传到服务器：

```text
submission/
eval/
tests/
jobs/
pokemon-tcg-ai-battle/
执行文档.md
M0_交付说明.md
```

其中 `submission/cg/` 必须包含 Linux 动态库：

```text
submission/cg/libcg.so
submission/cg/libcg-arm64.so
```

服务器如果是 x86_64 Linux，会用 `libcg.so`；本机 macOS 用的是 `libcg.dylib`。

不建议上传：

```text
experiments/
logs/
.DS_Store
__pycache__/
```

示例上传命令：

```bash
rsync -av --exclude '.DS_Store' --exclude '__pycache__' \
  /Users/hank/Desktop/pokemon/ \
  YOUR_USERNAME@172.23.160.47:/share/home/YOUR_USERNAME/pokemon/
```

## 5. 服务器环境配置

根据 `GPUUsing.docx`：

```bash
ssh YOUR_USERNAME@172.23.160.47

# 如果还没有 conda，先安装 /share/home 下的 Anaconda。
# 安装后重新登录，或执行：
source ~/.bashrc

conda create -n pokemon-tcg python=3.11 -y
conda activate pokemon-tcg

# M0 不需要额外 Python 包；官方引擎只依赖标准库 + 动态库。
python --version
python eval/run_match.py --agent0 submission --agent1 random --games 10
python tests/test_regression.py
```

如果服务器需要外网认证，按教程执行：

```bash
cp /share/home/.auth-setting ~
/share/home/auth_client -u YOUR_STUDENT_ID -p YOUR_PASSWORD auth
```

## 6. Slurm 提交

M0 CPU 评估使用：

```bash
sbatch jobs/m0_cpu_eval.slurm
```

提交前必须替换 `jobs/m0_cpu_eval.slurm` 中的：

```text
YOUR_USERNAME
YOUR_EMAIL
```

查询与取消：

```bash
squeue -u YOUR_USERNAME
scancel JOB_ID
scontrol show job JOB_ID
```

## 7. 后续哪些阶段需要算力服务器

| 阶段 | 是否建议上服务器 | 是否需要 GPU | 原因 |
|---|---|---:|---|
| M0 基准评估 | 可选 | 否 | 1000 局本机约十几秒；大批量回归可上服务器 CPU |
| M1 强规则 Agent | 可选 | 否 | 主要是规则开发和小批量评估 |
| M2 联赛评估/日志分析 | 建议 | 否 | 大量对局矩阵更适合 Slurm 批量跑 |
| M3 belief + 搜索 | 建议 | 否 | 当前 Search API 根搜索偏 CPU，GPU 不参与 |
| M4 卡组优化 | 建议 | 否 | 多卡组、多种子批量评估适合 CPU 作业阵列 |
| M5 卡组优化/协同进化 | 建议 | 否 | 当前 MAP-Elites 和快筛均为 CPU 流程 |
| M6 小模型蒸馏 | 可选 | 否 | 当前交付为 NumPy 线性 smoke 管线；只有后续神经网络版才需要 GPU |
| 最终冻结回归 | 建议 | 否 | 提交前大规模稳定性验证 |

服务器教程写明 `compute` 分区最多 2 张 GPU、最多 10G 内存、单任务最长 7 天。当前 M0-M6 与最终冻结默认均可使用 CPU 作业；不要为当前 NumPy/规则流程申请 GPU。
