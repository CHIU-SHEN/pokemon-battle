# M2 评估系统和实验纪律说明

更新时间：2026-07-06

## 1. 已完成内容

M2 已按 `执行文档.md` 完成：

- 对局矩阵：`eval/league.py`
- Wilson 置信区间与晋级/观察/淘汰标签：`eval/stats.py`
- replayable bad case 保存：`eval/run_match.py --bad-case-dir ...`
- bad case 复现与多 agent 动作比较：`eval/replay_case.py`
- CPU Slurm 模板：`jobs/m2_league.slurm`

固定基线池当前包含：

```text
Random
Sample
Exploiter-FirstMin
V0-current
V0-best
```

说明：当前 `V0-best` 暂时指向当前 V0。后续一旦冻结历史最佳版本，应把 `eval/league.py::BASELINES["V0-best"]` 改到冻结目录。

## 2. 常用命令

快筛矩阵：

```bash
python eval/league.py \
  --candidate submission \
  --games 500 \
  --baselines Random,Sample,Exploiter-FirstMin,V0-best \
  --include-self \
  --save-bad-cases
```

单个 summary 的统计标签：

```bash
python eval/stats.py experiments/20260706_m2_league_500/candidate_vs_Random/summary.json
```

复现坏样本并比较多个 agent：

```bash
python eval/replay_case.py logs/bad_cases/<run>/<baseline>/<case>.json \
  --agent submission \
  --agent pokemon-tcg-ai-battle/sample_submission/sample_submission/main.py
```

## 3. 本地验收结果

主验收矩阵：

```text
experiments/20260706_m2_league_500/league_report.json
```

| Candidate \ Baseline | Random | Sample | Exploiter-FirstMin | V0-best | V0-current |
|---|---:|---:|---:|---:|---:|
| submission | 0.902 (451-49-0) | 0.930 (465-35-0) | 0.790 (395-105-0) | 0.628 (314-186-0) | 0.622 (311-189-0) |

外部基线的自动标签：

| Baseline | Wilson 95% CI | 标签 |
|---|---:|---|
| Random | 0.873 - 0.925 | 晋级 |
| Sample | 0.904 - 0.949 | 晋级 |
| Exploiter-FirstMin | 0.752 - 0.823 | 晋级 |

同源 mirror/sanity：

| Baseline | 说明 |
|---|---|
| V0-best | 当前与 candidate 同源，只能观察先后手/随机性，不作为晋级证据 |
| V0-current | 当前与 candidate 同源，只能观察先后手/随机性，不作为晋级证据 |

自动标签逻辑已用 200 局矩阵验证：

```text
experiments/20260706_m2_league_200/league_report.json
```

其中 Random / Sample / Exploiter-FirstMin 标为“晋级”，V0-best / V0-current 标为“观察”。

## 4. Bad Case

500 局矩阵保存了 loss / exception / illegal / high-latency / max-step draw 类型的 bad case：

```text
logs/bad_cases/20260706_152704/
```

已验证可复现：

```bash
python eval/replay_case.py \
  logs/bad_cases/20260706_152704/Exploiter-FirstMin/20260706_152713_00008_agent0_loss.json \
  --agent submission \
  --agent pokemon-tcg-ai-battle/sample_submission/sample_submission/main.py \
  --limit 5
```

输出会列出每个关键 step 的：

- 原始动作；
- 新 agent 动作；
- 对照 agent 动作；
- 合法性检查结果。

## 5. 晋级纪律

建议沿用三层筛选：

| 阶段 | 局数 | 用途 |
|---|---:|---|
| 快筛 | 200-500 | 明显退化直接淘汰 |
| 中筛 | 1000-3000 | 判断稳定提升 |
| 终筛 | 5000-20000 | 对主候选做更窄置信区间 |

自动标签规则：

- 有异常或非法动作：淘汰；
- 少于 200 局：观察；
- 胜率超过基线 2%，且 Wilson 下界高于基线：晋级；
- Wilson 上界低于基线：淘汰；
- 其他：观察。

注意：同源 mirror 不应作为晋级证据，主要用于发现先后手偏差和明显退化。

## 6. 算力服务器

M2 仍不需要 GPU，适合 CPU Slurm 批量跑矩阵。提交：

```bash
sbatch jobs/m2_league.slurm
```

提交前替换：

```text
YOUR_USERNAME
YOUR_EMAIL
```

环境沿用 M0/M1：

```bash
conda create -n pokemon-tcg python=3.11 -y
conda activate pokemon-tcg
python tests/test_fallback.py
python tests/test_regression.py
python eval/league.py --candidate submission --games 1000 --baselines Random,Sample,Exploiter-FirstMin,V0-best --include-self --save-bad-cases
```

