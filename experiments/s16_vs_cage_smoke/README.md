# S16 vs Cage rules smoke

Local, small-sample comparison artifacts. Both agents use the frozen
`crustle_kangaskhan_cage` 60-card deck. Results are split by seat because the
bundled engine does not expose deterministic RNG seed control.
